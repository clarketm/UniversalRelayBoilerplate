/*eslint no-unused-vars: ["error", { "args": "none" }]*/
export default function serverExtensions( router )
{
  // Here [router] is the main router. If you need to attach additional routes to it, do it below
}
